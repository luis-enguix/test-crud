# test-crud
test crud
 Para que el proyecto funcione de forma local, hay que importar el archivo ".sql"  que esta en la 
 carpeta llamada database.
 
 En el archivo llamado "db.php" hay que modificar los datos que se tengan configurados para localhost para que
 funcione en el ordenador que se vaya a correr.
